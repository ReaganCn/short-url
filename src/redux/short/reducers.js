import {  } from "./types";


const defaultState = {
    url: "",
    shorturl: ""
}

const shorteningReducer = ( state = defaultState, action) => {
        switch (action.type) {

            default:
                return state
        }
}

export {shorteningReducer}